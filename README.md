### Запуск первого дз
```
npm run dev
```

### Запуск второго дз
```
npm run timer
```
```
### Первое задание:
Первым запускается синхронный код (фаза poll) - 1, 5
Далее в очередь попадают Promise, SetTimeout (асинхронный код) =>
Первым попадает в очередь (микро задача) Promise и Promise внутри него - 6
Вторым в очередь попадает (макро задача) SetTimeout, и выводится асинхронный код - 2
Далее в setTimeout срабатывает Promise, 
    внутри которого отрабатывает setTimeout и выводит синхронный код - 3
    после чего срабаотывает Promise и выводит - 4
### Второе задание:
Ввести дату события не раньше текущей даты в формате "2021-12-13/20:01:13"
где первым значением идет год, после месяц и третьим день. Всё это разделяется символом "тире".
Формат времени отделяется от даты "слешем", после чего  через "двоеточие" идут - часы, минуты, секунды.
```
